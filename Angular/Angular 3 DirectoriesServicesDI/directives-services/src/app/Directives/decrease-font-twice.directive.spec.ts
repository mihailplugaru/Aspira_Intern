import { DecreaseFontTwiceDirective } from './decrease-font-twice.directive';

describe('DecreaseFontTwiceDirective', () => {
  it('should create an instance', () => {
    const directive = new DecreaseFontTwiceDirective();
    expect(directive).toBeTruthy();
  });
});
