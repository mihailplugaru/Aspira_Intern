import { IncreaseFontTwiceDirective } from './increase-font-twice.directive';

describe('IncreaseFontTwiceDirective', () => {
  it('should create an instance', () => {
    const directive = new IncreaseFontTwiceDirective();
    expect(directive).toBeTruthy();
  });
});
